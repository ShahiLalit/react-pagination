import React from "react";
import { IRowsCountProps } from "./RowsCount.types";
declare const RowsCount: ({ options, onSelection, numberOfRows, }: IRowsCountProps) => React.JSX.Element;
export default RowsCount;
